package Interface;

public interface IProyect {
    // Definir el modelo y poco mas. Change creator tiene que buscar en el repo de usuarios la coincidencia. No obstante,
    // lo hablamos
    public boolean changeCreator(String User);
}